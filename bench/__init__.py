"""Performance benchmark suite for Winnow.

Run it with `python3 -m bench` from the repo root; see bench/README.md.
Kept out of tests/ on purpose — pytest.ini's testpaths only covers tests/,
and a `pytest` run should stay a correctness run measured in seconds.
"""
